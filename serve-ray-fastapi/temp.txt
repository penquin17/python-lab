docker pull continuumio/miniconda3

docker build -f dockerfile -t python-lab:serve-ray-fastapi .
docker run -it -v "./":"/code" -p 8000:8000 python-lab:serve-ray-fastapi /bin/bash

uvicorn app.src.runapp:app --host 0.0.0.0 --port 8000 --reload


import requests
url = "http://localhost:8000/v1/gen_music"
data = {"text": "Heal the world"}
response = requests.post(url, json=data)